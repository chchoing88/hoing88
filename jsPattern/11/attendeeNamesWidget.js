var Conference = Conference || {};
Conference.Widgets = Conference.Widgets || {};

Conference.Widgets.attendeeNamesWidget = function () {
  'use strict'

  // 해당 도구를 사용할 수 없으면 즉시 실패처리를 한다.
}

Conference.Widgets.messages = {
  missingTool: "누락된 도구: "
}